#!/usr/bin/env ruby
# The command line Haml parser.

path = File.dirname(File.symlink?(__FILE__) ? File.readlink(__FILE__) : __FILE__) + '/../lib'
$LOAD_PATH.unshift path
require 'ggen'
require 'ggen/exec'

opts = Ggen::Exec::Ggen.new(ARGV)
opts.parse!
